/*#include <stdio.h>
#include <stdlib.h>
#include "ListUtas.h"


void CreateUtas(ListUtas *U, int Capacity)
{  
    FIRST(U) = NULL;
}

void InputUtas(ListUtas *U)
{

}

boolean IsIdxFound(ListUtas U){
    Address p = FIRST(U);
    boolean isFound  = FALSE;
    while (NEXT(p) != NULL)
    {
        if()
        p = NEXT(p);
    }
    
}


*/